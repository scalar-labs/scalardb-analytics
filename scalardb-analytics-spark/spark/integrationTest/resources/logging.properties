# Java Util Logging configuration for integration tests
# This file suppresses verbose logging from libraries that use java.util.logging directly

# Global logging level - only show warnings and above
.level=WARNING

# Suppress SQL Server JDBC driver logging
# The driver uses java.util.logging for internal connection details
com.microsoft.sqlserver.jdbc.level=OFF
